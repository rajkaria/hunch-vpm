{
  "name": "@hunch-vpm/subgraph",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "private": true,
  "description": "The Graph index for the Hunch VPM settlers, resolver and factory on Arc",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "typecheck": "graph codegen && graph build",
    "test": "graph test",
    "deploy:testnet": "graph deploy hunch-vpm --network arc-testnet --node https://subgraphs.thegraph.com/api/deploy --ipfs https://ipfs.thegraph.com",
    "deploy:studio": "graph deploy hunch-vpm --network arc-testnet"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2"
  },
  "devDependencies": {
    "assemblyscript": "0.19.23",
    "matchstick-as": "0.6.0"
  }
}
