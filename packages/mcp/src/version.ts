/**
 * The version reported to MCP clients. Kept in sync with package.json by a test rather
 * than by importing the manifest, which would drag package.json into the build output.
 */
export const VERSION = "0.1.0";

export const SERVER_NAME = "hunch-vpm";
