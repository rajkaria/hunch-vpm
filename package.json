{
  "name": "hunch-vpm",
  "version": "0.1.0",
  "private": true,
  "description": "The Vested Parimutuel, live on Arc",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "verify": "bash scripts/verify.sh",
    "preflight:testnet": "bash scripts/preflight-deploy.sh arc-deployer testnet",
    "wire:testnet": "node scripts/wire-deployment.mjs testnet",
    "wire:mainnet": "node scripts/wire-deployment.mjs mainnet",
    "wire:check": "node scripts/wire-deployment.mjs --check",
    "contracts:build": "forge build --root contracts",
    "contracts:test": "forge test --root contracts"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "next": "16.3.5"
  }
}
